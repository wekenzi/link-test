import { ChangeColorHoverDirective } from './change-color-hover.directive';

describe('ChangeColorHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangeColorHoverDirective();
    expect(directive).toBeTruthy();
  });
});
